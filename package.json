{
  "name": "ona-experiences",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @ona/web",
    "dev:api": "npm run dev -w @ona/api",
    "build": "npm run build -w @ona/web && npm run build -w @ona/api",
    "build:web": "npm run build -w @ona/web",
    "build:api": "npm run build -w @ona/api"
  }
}
