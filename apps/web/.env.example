# Leave empty in development to use the Vite /api proxy (recommended).
# Set the public API URL for production builds (e.g. https://api.example.com).
VITE_API_URL=

# Backend target for the Vite dev proxy (must match apps/api PORT).
VITE_API_PROXY_TARGET=http://localhost:3001
